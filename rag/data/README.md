# Data Folder

Place source files here before running `python ../ingest.py` from the `rag/` folder.

Supported file types:

- `.txt`
- `.md`
- `.markdown`

The ingestion script chunks these files and stores their embeddings in `rag/chroma_store/`.

