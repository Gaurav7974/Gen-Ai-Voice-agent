# Chroma Store

The local persistent ChromaDB files will be written here when you run:

```bash
python ingest.py
```

Do not commit generated database files unless you intentionally want to version the local vector store.

